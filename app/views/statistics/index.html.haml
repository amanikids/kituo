%h1= t('statistics.index.title')

%h2= t('statistics.length_of_stay.title')
= tag(:object, :type => Mime::SVG, :data => length_of_stay_statistics_path(:format => :svg), :width => 400, :height => 200)
