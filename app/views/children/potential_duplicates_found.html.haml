%h1 Potential Duplicates Found
%h2 Did you mean one of these children?

- @child.potential_duplicates.each do |child|
  - form_for [child, @child.arrivals.last], :html => { :class => 'headshot' } do |form|
    .hidden
      = form.date_select :happened_on, {}, { :style => 'display:none' }
    .submit
      = image_submit_tag child.headshot.url, :alt => "Record arrival on #{form.object.happened_on.to_s(:long)}"
      = child.name

- form_for @child do |form|
  .hidden
    = form.hidden_field :name
    - form.fields_for :arrivals do |arrival_form|
      = arrival_form.date_select :happened_on, {}, { :style => 'display:none' }

  .submit
    Or, if you're sure,
    = form.submit "Create a New Case File for #{h @child.name}", :name => 'child[ignore_potential_duplicates]'