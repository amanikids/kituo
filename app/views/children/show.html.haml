%h1= @child.name

.headshot
  = image_tag @child.headshot.url
  = link_to 'Edit Headshot', edit_child_headshot_path(@child)

%ul
  - @child.arrivals.each do |arrival|
    %li= arrival.happened_on.to_s(:long)
