%h1= @child.name

.headshot
  = image_tag @child.headshot.url
  = link_to 'Edit Headshot', edit_child_headshot_path(@child)

%h2 Timeline

%ul.actions
  %li Record:
  %li= link_to 'Reunification', new_child_reunification_path(@child)

%table.timeline
  = render @child.events
