%h1
  = link_to @child.name, @child

%h2 Offsite Boarding

- form_for [@child, @offsite_boarding] do |form|
  .field
    = form.label :happened_on, 'Boarded on'
    = form.date_select :happened_on
  .submit
    = form.submit 'Save'
