!!! XML
!!!

%html{html_attrs}
  %head
    = stylesheet_link_tag 'reset', controller_name
    = yield(:css)
    - js 'change_locale'
    = javascript_tags
    - other_locales.each do |code, name|
      %link{ :rel => 'alternate', :hreflang => code, :title => name, :href => url_for(:locale => code) }

  %body
    = yield

    #ajax-loading
      Working
