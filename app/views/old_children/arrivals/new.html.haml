= render :partial => 'children/header', :locals => { :child => @child }
%h2= Arrival.human_name

- form_for [@child, @arrival] do |form|
  .field
    = form.label :happened_on
    = form.date_select :happened_on
  .submit
    = form.submit t('children.arrivals.new.submit')
