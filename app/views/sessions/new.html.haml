%h1= t('.heading')
%h2= t('.subheading')

%ul#caregivers
  - @caregivers.each do |caregiver|
    %li= link_to(image_tag(caregiver.headshot.url) + h(caregiver.name), session_path(:user_id => caregiver.id), :method => 'post')

%p#not_here
  = t('.not_here')
  = link_to t('.tell_us_who_you_are'), new_caregiver_path

%ul#locales
  - other_locales.each do |locale, locale_name|
    %li= link_to image_tag("#{locale}.png") + ' ' + locale_name, params.merge(:locale => locale)
