%h1
  = link_to @child.name, @child

%h2= Reunification.human_name

- form_for [@child, @reunification] do |form|
  .field
    = form.label :happened_on
    = form.date_select :happened_on
  .submit
    = form.submit t('reunifications.new.submit')
