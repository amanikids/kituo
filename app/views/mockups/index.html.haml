.hello
  %h1 Hello!
  %h2 Who are you?

  %ul.caregivers
    - Caregiver.by_name.each do |caregiver|
      %li
        = link_to(image_tag(caregiver.headshot.url), :action => 1)
        = link_to(caregiver.name, :action => 1)
  %ul#locales
    - other_locales.each do |locale, locale_name|
      %li= link_to image_tag("#{locale}.png") + ' ' + locale_name, params.merge(:locale => locale), :method => (request.get? ? nil : request.method)
